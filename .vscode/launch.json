{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run FastAPI (Uvicorn)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "app.main:app",
        "--reload"
      ],
      "cwd": "${workspaceFolder}",
      "env": {
        "DATABASE_URL": "postgresql://dispatchdesk:dispatchdesk@localhost:5432/dispatchdesk"
      },
      "console": "integratedTerminal"
    }
  ]
}